import React from "react"

const block = ({text}) => {
  return (
    <span className="bg-richblack-700 text-white pr-[0.2vw] pl-[0.2vw] py-[0.2vh] rounded outline ml-[0.3vw]">{text}</span>
  )
};

export default block;
